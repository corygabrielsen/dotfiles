# save each command's beginning timestamp and duration
setopt extended_history
# add new history lines incrementally rather than waiting until the shell exits
setopt inc_append_history
setopt hist_expire_dups_first # unique events are more useful
setopt hist_verify # echo when calling !! rather than executing
setopt share_history

